version: 1.2.0.{build}

branches:
  only:
  - master

skip_tags: true

image: Visual Studio 2022
configuration: Release

clone_depth: 1
clone_folder: C:\build

build:
  project: SDK-CPP.sln
  parallel: true
  verbosity: minimal